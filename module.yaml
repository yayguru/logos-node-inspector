{
  "name": "node_inspector",
  "version": "0.1.0",
  "type": "core",
  "category": "operator",
  "description": "Read-only Basecamp module for inspecting a single Linux VPS that runs logos-node and nwaku.",
  "nix_packages": {
    "runtime": [
      "openssh"
    ],
    "build": [
      "cmake",
      "ninja",
      "pkg-config"
    ]
  }
}
